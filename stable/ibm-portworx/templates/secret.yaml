{{- if .Values.apikey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.apikeySecretName }}
  labels:
    {{- include "ibm-portworx.labels" . | nindent 4 }}
stringData:
  apikey: {{ .Values.apikey }}
{{- end }}
