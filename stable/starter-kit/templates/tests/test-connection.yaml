apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "starter-kit.fullname" . }}-test-connection"
  labels:
    {{- include "starter-kit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "starter-kit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
