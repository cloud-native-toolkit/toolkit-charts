apiVersion: operator.knative.dev/v1alpha1
kind: KnativeServing
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
spec:
    high-availability:
        replicas: {{ .Values.replicaCount }}
