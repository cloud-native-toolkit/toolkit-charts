# Default values for gitea.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
# Default values for argocd-operator.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
global:
  clusterType: "ocp4"

nameOverride: ""

giteaInstance:
  name: gitea-tools
  namespace: tools
  giteaSsl: true
  # can not use admin for giteaAdminUser
  giteaAdminUser: toolkit
  giteaAdminPassword: toolkit
  giteaAdminEmail: toolkit@cloudnativetoolkit.dev
