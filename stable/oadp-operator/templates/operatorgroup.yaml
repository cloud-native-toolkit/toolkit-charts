apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ .Values.subscriptions.operatorgroup.name }}
  namespace: {{ .Values.subscriptions.operatorgroup.namespace }}
spec:
  targetNamespaces:
    - {{ .Values.subscriptions.operatorgroup.namespace }}