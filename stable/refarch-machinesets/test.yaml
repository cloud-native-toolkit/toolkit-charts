infrastructureId: "ocpinstall-s6nzf"
# cloudProvider.name set to aws, azure, ibmcloud or vsphere
# cloudProvider.managed set to true for ROSA, ARO, ROKS
cloudProvider:
  name: "vsphere" 
  managed: false
# only for "aws", "ibmcloud" or "azure" maybe "gcp"
cloud:
  region: ${REGION}
  image: ${IMAGE_NAME}
# only for "vsphere" - ignored for other
vsphere:
  networkName: gym-50b01ssfct-z60c91ei-segment
  datacenter: IBMCloud
  datastore: gym-50b01ssfct-z60c91ei-storage 
  cluster: ocp-gym 
  server: ocpgym-vc.techzone.ibm.local
  folder: "/IBMCloud/vm/ocp-gym/gym-50b01ssfct-z60c91ei"
  resourcePool: >-
    /IBMCloud/host/ocp-gym/Resources/Cluster Resource Pool/Gym Member
    Resource Pool/gym-50b01ssfct-z60c91ei